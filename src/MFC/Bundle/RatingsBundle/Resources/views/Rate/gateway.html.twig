{% extends 'MFCRatingsBundle::gatewayLayout.html.twig' %}

{% block title %}
{% if maplet is defined %}
{{ maplet.name }}
{% else %}
Rating Maplets
{% endif %}
{% endblock %}

{% block css %}
{{ parent() }}
<style>
	a.link-block, a.link-block:hover, a.link-block:active {
	    color: inherit;
	    text-decoration: none;
	}

	a.link-block .panel {
	    background-color: #f3f3f3;
	}

	a.student .panel {
	    background-color: #FF87C3;
	}
	a.student .panel:hover {
	    background-color: #E67AB0;
	}

	a.instructor .panel {
	    background-color: #5BED6B;
	}
	a.instructor .panel:hover {
	    background-color: #51D460;
	}
</style>
{% endblock %}

{% block main %}
	<div class="page-header">
		<p class="lead">Welcome to the Maplets rating page.
		Your responses will be used to create statistics that may be displayed to other users, but no identifying personal data will be collected or displayed.</p>
                {% if maplet is defined %}
		<p class="lead">Thank you for choosing to provide feedback on the Maplet:
                <strong style="display:block; text-align:center">{{ maplet.name }}</strong></p>
		{% endif %}
                <p class="lead">Before we continue, please let us know whether you are a student in the course or an instructor teaching the course.</p>	
	</div>
	<div class="row">
		<div class="col-sm-6">
			{% if slug is defined %}
			<a href="{{ path('set_role_cookie_slug', {'role': 'student', 'backtrack': backtrack, 'slug': slug }) }}" class="link-block student">
			{% else %}
			{% if maplet is defined %}
			<a href="{{ path('page_maplet_3', {'slug': maplet.slug, 'role': 'student'}) }}" class="link-block student">
			{% else %}
			<a href="{{ path('set_role_cookie', {'role': 'student', 'backtrack': backtrack }) }}" class="link-block student">
			{% endif %}
			{% endif %}
				<div class="panel panel-default">
					<div class="panel-body">
						<h2>Student</h2>
						<p>You will be asked if you have had your test yet, if you learned from the maplet, and if will be or was useful for the test. You can also leave comments.</p>
					</div>
				</div>
			</a>
		</div>
		<div class="col-sm-6">
			{% if slug is defined %}
			<a href="{{ path('set_role_cookie_slug', {'role': 'instructor', 'backtrack': backtrack, 'slug': slug }) }}" class="link-block instructor">
			{% else %}
			{% if maplet is defined %}
			<a href="{{ path('page_maplet_3', {'slug': maplet.slug, 'role': 'instructor'}) }}" class="link-block instructor">
			{% else %}
			<a href="{{ path('set_role_cookie', {'role': 'instructor', 'backtrack': backtrack }) }}" class="link-block instructor">
			{% endif %}
			{% endif %}
				<div class="panel panel-default">
					<div class="panel-body">
						<h2>Instructor</h2>
						<p>You will be asked how you use the maplet, what you think your students learn, and your overall opinion of the maplet. You can also leave comments.</p>
					</div>
				</div>
			</a>
		</div>
	</div>
{% endblock %}
